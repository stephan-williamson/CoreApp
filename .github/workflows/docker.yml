name: CorApp Docker Series Build

on: [push]

jobs:
    build:
      runs-on: ubuntu-latest
      steps:
      - uses:  actions/checkout@v4
      - name:  docker build csharp
        run:  |
          docker build -t coreapp:debug -f CoreApp/Dockerfile .
